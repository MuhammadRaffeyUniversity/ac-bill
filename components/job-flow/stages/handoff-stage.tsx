import { CustomerLinkActions } from "@/components/billing/customer-link-actions";

export function HandoffStage({ invoiceNumber, total, paid, invoicePath, pdfPath, feedbackPath }: { invoiceNumber: string; total: number; paid: number; invoicePath: string; pdfPath: string; feedbackPath: string }) {
  return <section className="rounded-xl border bg-card p-5"><p className="font-mono text-xs uppercase tracking-widest text-muted-foreground">Stage 5 of 5</p><h2 className="mt-1 text-xl font-semibold">Customer handoff</h2><p className="mt-1 text-sm text-muted-foreground">Invoice {invoiceNumber} · RM {total.toFixed(2)} total · RM {Math.max(0, total - paid).toFixed(2)} due</p><div className="mt-5 flex flex-wrap gap-2"><a href={invoicePath} target="_blank" rel="noreferrer" className="inline-flex h-9 items-center rounded-md border px-3 text-sm font-medium hover:bg-muted">Open customer invoice</a><a href={pdfPath} className="inline-flex h-9 items-center rounded-md bg-primary px-3 text-sm font-medium text-primary-foreground">Download PDF</a></div><CustomerLinkActions feedbackPath={feedbackPath} invoicePath={invoicePath} /></section>;
}
